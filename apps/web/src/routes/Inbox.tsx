import { SectionHeader } from '../components/SectionHeader'

// Les 5 panneaux de traitement de l'inbox sont la Task 7. Cette page ne pose
// que la route et son label de section.
export function Inbox() {
  return <SectionHeader label="Inbox" />
}
