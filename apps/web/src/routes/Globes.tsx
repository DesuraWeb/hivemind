import { SectionHeader } from '../components/SectionHeader'

// Le hub système solaire des globes est la Task 8. Cette page ne pose que la
// route et son label de section.
export function Globes() {
  return <SectionHeader label="Globes" />
}
