import { SectionHeader } from '../components/SectionHeader'

// Écran hors périmètre de la Phase 3 (voir le plan). Cette page ne pose que
// la route et son label de section.
export function Reglages() {
  return <SectionHeader label="Réglages" />
}
