import { SectionHeader } from '../components/SectionHeader'

// Le contenu du dashboard (brief du matin, orbe, boucles) est la Task 8.
// Cette page ne pose que la route et son label de section.
export function Dashboard() {
  return <SectionHeader label="Dashboard" />
}
